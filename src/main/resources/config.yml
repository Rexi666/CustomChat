messages:
  config_reload: "&aConfiguration reloaded successfully!"
  usage: "&cUsage: /customchat reload"
  no_permission: "&cYou do not have permission to use this command."
  k_blocked: "&cYou cannot use &k in chat messages."

# colorchat command, allows players to decide the color of their chat messages.
# Players need customchat.colorchat.color.<color> permission to use a specific color.
# And customchat.colorchat.gradient.<gradient> for gradients.
colorchat:
  enabled: true
  gradients_enabled: true
  colors:
    black: true
    dark_blue: true
    dark_green: true
    dark_aqua: true
    dark_red: true
    dark_purple: true
    gold: true
    gray: true
    dark_gray: true
    blue: true
    green: true
    aqua: true
    red: true
    light_purple: true
    yellow: true
    white: true

block_k: true #Block &k in chat messages, preventing players from using the magic formatting code.

formats:
# Higher priority formats will override lower ones. Top ones are checked first.
  staff:
    # permission to use this format
    permission: "customchat.format.staff"
    # display name of the format
    display: "%luckperms_prefix% &f{displayName} %luckperms_suffix%"
    two_points: "&8»"
    # The color of the message sent. Can use color codes or minimessage, including gradients <gradient:#HEX1:#HEX2>
    message: "&f{message}"
    # Message that appears when hovering over the message
    hover:
      message:
        - ' &8&m───────────────'
        - ' &f💬 &7Server Staff &f💬'
        - ' &8&m───────────────'
        - ' &eℹ Information:'
        - '    &7Name: &r&f{displayName}'
        - '    &7Rank: %luckperms_prefix%'
        - '    &7Money: %vault_eco_balance_formatted%$'
        - ' &8&m───────────────'
      # The command to run when clicking the message
      click_action:
        # Types: "run_command", "suggest_command", "open_url"
        type: "suggest_command"
        value: "/msg {displayName}"
  default:
    permission: ""
    display: "&7{displayName} %luckperms_suffix%"
    two_points: "&8»"
    message: "<gray>{message}"
    hover:
      message:
        - ' &8&m───────────────'
        - ' &eℹ Information:'
        - '    &7Name: &r&f{displayName}'
        - '    &7Money: %vault_eco_balance_formatted%$'
        - ' &8&m───────────────'
      click_action:
        type: "suggest_command"
        value: "/msg {displayName}"